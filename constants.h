#define EYELID_CLOSED 0
#define EYELID_SQUINT 30
#define EYELID_NORMAL 80
#define EYELID_FULL 100

int LEL_TRIM = 0; // Positive = more open
int LEU_TRIM = 0; // Negative = more open
int REU_TRIM = -5; // Positive = more open 
int REL_TRIM = -15; // Negative = more open

int UD_TRIM = -25; // Negative = Down
int LR_TRIM = 0;  